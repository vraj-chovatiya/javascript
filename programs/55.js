// optional chaining

const user = {
  firstname: "vraj",
  // address: {houseNumber: '123'}
}

// console.log(user?.address?.houseNumber);
console.log(user?.firstname);
console.log(user?.address?.houseNumber);
